# Use this for Railway / Render / free-tier deploy (no OpenCV/MediaPipe/scikit-learn).
# Pip: pip install -r requirements-light.txt
# Python 3.11 recommended (set in runtime.txt or Dockerfile).

fastapi==0.109.0
uvicorn[standard]==0.27.0
python-multipart==0.0.6
pydantic==2.6.0
pydantic-settings==2.1.0
websockets==12.0
motor==3.3.2
python-dotenv==1.0.0
aiofiles==23.2.1
